import { ResizecolDirective } from './resizecol.directive';

describe('ResizecolDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizecolDirective();
    expect(directive).toBeTruthy();
  });
});
